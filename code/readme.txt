Alpaca model has the code to run this chatbot
Conversation logs contain conversations of different models
Fine-tuning GPT3.5 turbo (with memory) contains code and data needed to fine-tune the model
Tone analysis contains code and data to fine-tune BERT and perform the analysis of models responses
User survey contains the survey form, feedback and code to generate the quantitative results